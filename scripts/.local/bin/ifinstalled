#!/bin/sh

# Check if commands exist without relying on a specific package manager (pacman/xbps/nix)
for x in "$@"; do
	if ! command -v "$x" >/dev/null 2>&1; then
 		notify-send "📦 $x" "must be installed for this function." && exit 1
	fi
done